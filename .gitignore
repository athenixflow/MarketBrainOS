# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase / build artifacts & local secrets
functions/lib
functions/node_modules
.env
.env.*
!.env.example
.firebase

# raw AI-generated media candidates (regenerate with `npm run media:generate`)
media-src/

# Claude Code local-only settings (machine-specific, not part of the project)
.claude/settings.local.json

# The GTM playbook. THIS REPOSITORY IS PUBLIC and docs/gtm is the whole go-to-market
# strategy, pricing reasoning, competitor analysis and channel plan. It has survived this
# long by nobody running `git add -A` on a day they were in a hurry; this makes that safe.
docs/gtm/
